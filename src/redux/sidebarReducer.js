let initialState = {
  friends: [
    {id: 1, name: 'John'},
    {id: 2, name: 'Peter'},
    {id: 3, name: 'Maria'}
  ]
};

const sidebarReducer = (state = initialState, action) => {


  return state;
}

export default sidebarReducer;